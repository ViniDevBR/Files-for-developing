export interface IModal {
  visible: boolean
  onClose: VoidFunction
}

//Interface para extender usar e nao ficar copiando e colando, reaproveitando codigo e caso queira outras props, basta extender essa
